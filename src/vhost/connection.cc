#include "connection.hh"

#include <events/sendv.hh>
#include <string>

#include "../main.hh"
#include "dispatcher.hh"

namespace http
{
    /*Connection::Connection(shared_socket sock, Request request,
                           Response response)
        : sock_{sock}
        , req_{request}
        , rep_{response}
    {
        http::dispatcher.dispatch_request(*this);
    }*/

} // namespace http